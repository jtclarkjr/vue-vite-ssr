services:
  app:
    build:
      context: .
      target: runtime
    image: vue-vite-ssr:local
    environment:
      PORT: ${PORT:-8080}
    ports:
      - '${PORT:-8080}:${PORT:-8080}'
    restart: unless-stopped
